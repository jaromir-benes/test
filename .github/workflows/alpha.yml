name: alpha

on:
  push:
    tags:
      - 'release*'

  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    
    - name: Clone master
      uses: actions/checkout@master
      #with:
        #ref: ${{ github.ref }}      
        #fetch-depth: 0
        
    - name: List tags
      run: |
        echo "${{ github.ref }}"
        echo "--1--"
        ls -al .git/refs/tags
        echo "--2--"
        cd .git/refs/tags
        echo "--3--"
        pwd
        echo "--4--"
        ls -al

